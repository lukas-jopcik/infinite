{
  "name": "nasa-fetcher",
  "version": "1.0.0",
  "description": "Fetches NASA APOD (daily) or RSS and invokes content processor",
  "main": "index.js",
  "scripts": {
    "zip": "zip -r nasa-fetcher.zip ."
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "xml2js": "^0.6.2"
  }
}
